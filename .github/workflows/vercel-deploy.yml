name: Vercel Deploy

on:
  push:
    branches:
      - dev
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 18.x
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
      - name: Install Vercel CLI
        run: npm install -g vercel
      - name: Deploy to Vercel Dev
        if: github.ref == 'refs/heads/dev'
        run: vercel deploy --prebuilt --token ${{ secrets.VERCEL_TOKEN_DEV }} --env VERCEL_ENV=dev --prod
      - name: Deploy to Vercel Prd
        if: github.ref == 'refs/heads/main'
        run: vercel deploy --prebuilt --token ${{ secrets.VERCEL_TOKEN_PRD }} --env VERCEL_ENV=prd --prod